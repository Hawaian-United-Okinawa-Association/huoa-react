//import { CREATE_SUBSCRIPTION, FETCH_SUBSCRIPTIONS, DELETE_SUBSCRIPTION } from '../actions/actionTypes';

export default (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
