//import { CREATE_EVENT, FETCH_EVENT, FETCH_EVENTS, EDIT_EVENT, DELETE_EVENT } from '../actions/types';

export default (state = {}, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
