//import { CREATE_NEWS, FETCH_NEWS, FETCH_ALL_NEWS, EDIT_NEWS, DELETE_NEWS } from '../actions/types';

export default (state = {}, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
